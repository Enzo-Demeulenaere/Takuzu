SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'Takuzu',
      #onConflict : #useLoaded,
      #onUpgrade : #useLoaded,
      #ignoreImage : true,
      #directory : 'src',
      #platforms : [ #pharo ]
    }
  ],
  #testing : {
    #include : {
      #packages : [ 'Takuzu-Tests.*' ]
    }
}
}
